//Complete the solution so that it reverses the string passed into it.

//'world'  =>  'dlrow'
//'word'   =>  'drow'


function solution(str){
  var letters = str.split("");
  var  reverseArray = letters.reverse();
  var together = reverseArray.join("");
  return together;
}